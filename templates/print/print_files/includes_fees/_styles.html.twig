<style type="text/css">
    * {
        margin: 0;
        padding: 0;
        border: 0;
        font: inherit;
        font-size: 100%;
        vertical-align: baseline;
    }

    table {
        border-collapse: collapse;
        border-spacing: 0;
    }

    caption, th, td {
        text-align: left;
        font-weight: normal;
        vertical-align: middle;
    }

    a img {
        border: none;
    }

    body {
        font-size: 12px;
        margin: 0;
        padding: 0;
        color: #777777;
    }

    body a {
        text-decoration: none;
        color: inherit;
    }

    body a:hover {
        color: inherit;
        opacity: 0.7;
    }

    body .container {
        min-width: 500px;
        margin: 0 auto;
        padding: 0 30px;
    }

    body .clearfix:after {
        content: "";
        display: table;
        clear: both;
    }

    body .left {
        float: left;
    }

    body .right {
        float: right;
    }

    header {
        height: 30px;
        margin-top: 20px;
        margin-bottom: 40px;
        padding: 0px 5px 0;
    }

    header figure {
        float: left;
        width: 40px;
        margin-right: 10px;
    }

    header figure img {
        height: 40px;
    }

    header .company-info {
        color: #656464;
    }

    header .company-info .title {
        margin-bottom: 5px;
        color: #656464;
        font-size: 2em;
    }

    header .company-info .line {
        display: inline-block;
        height: 9px;
        margin: 0 4px;
        border-left: 1px solid #656464;
    }

    section .details {
        min-width: 500px;
        margin-bottom: 40px;
        padding: 10px 35px;
        color: #656464;
        border-top: 2px solid #656464;
        border-bottom: 2px solid #656464;
    }

    section .details .client {
        line-height: 20px;
    }

    section .details .client .name {
        font-size: 1.1em;
    }

    section .details .data {
        width: 50%;
        text-align: right;
    }

    section .details .title {
        margin-bottom: 15px;
        font-size: 1.6em;
        text-transform: uppercase;
    }

    section .details .date {
        font-size: 1em;
        text-transform: uppercase;
    }

    section .table-wrapper {
        position: relative;
        overflow: hidden;
    }

    section .no-break {
        page-break-inside: avoid;
    }

    section table {
        width: 100%;
        margin-bottom: -20px;
        table-layout: fixed;
        border-collapse: separate;
    }

    section table .no {
        width: 50px;
    }

    section table .desc {
        width: 55%;
    }

    section table .qty, section table .unit, section table .total {
        width: 35%;
    }

    section table tbody.head {
        vertical-align: middle;
        border-color: inherit;
    }

    section table tbody.head th {
        text-align: center;
        color: white;
        text-transform: uppercase;
    }

    section table tbody.head th div {
        display: inline-block;
        border-bottom: 2px solid #656464;
        color: #656464;
        width: 100%;
    }

    section table tbody.head th.desc div {
        width: 115px;
        margin-left: 0px;
    }

    section table tbody.body td {
        padding: 10px 5px;
        background: #F3F3F3;
        text-align: center;
    }

    section table tbody.body h3 {
        margin-bottom: 5px;
        color: #656464;
    }

    section table tbody.body .no {
        color: #656464;
        border: 2px solid #656464;
        background-color: white;
        font-size: 1.2em;
    }

    section table tbody.body .desc {
        padding-top: 0;
        padding-bottom: 0;
        background-color: transparent;
        color: #777787;
        font-size: 1.3em;
        text-align: left;
    }

    section table tbody.body .total {
        color: #656464;
        font-size: 1.3em;
    }

    section table tbody.body tr.total td {
        color: #656464;
        text-align: right;
    }

    section table tbody.body tr.total .total {
        font-size: 1.3em;
        color: #656464;
    }

    section table.grand-total {
        margin-top: 40px;
        margin-bottom: 40px;
    }

    section table.grand-total tbody td {
        padding-top: 15px;
        background-color: white;
        color: #656464;
        text-align: center;
    }

    section table.grand-total tbody td.no,
    section table.grand-total tbody td.qty {
        background-color: transparent;
    }

    section table.grand-total tbody td.desc {
        color: #656464;
        font-size: 1.16em;
        text-transform: uppercase;
    }

    section table.grand-total tbody td.total,
    section table.grand-total tbody td.grand-total {
        border-bottom: 2px solid #656464;
        font-size: 1.3em;
        vertical-align: bottom;
    }

    section table.grand-total tbody td.grand-total {
        font-size: 1.16em;
        background-color: transparent;
        text-align: center;
    }


    footer {
        position: relative;
        top: 180px;
        padding: 0 5px;
    }

    footer .notice {
        /*margin-bottom: 25px;*/
        font-size: 1.16666666666667em;
    }

    footer .end {
        padding-top: 5px;
        border-top: 2px solid #656464;
        text-align: center;
        font-size: 1.16666666666667em;
    }

</style>